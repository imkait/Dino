# 交接檔（handoff.md）

> 任何 Agent、任何電腦接手前必讀；收工時必更新。

## ⏯️ 目前做到哪
已完成遊戲企劃與最小可玩原型，並新增道具系統，包含護盾與雙倍分數效果。

## 🚦 目前狀態
可直接以瀏覽器開啟 `index.html` 遊玩；CSS 位於 `styles.css`，遊戲邏輯位於 `game.js`，支援奔跑動畫、跳躍、撞擊圖、障礙物、道具拾取、護盾抵擋、雙倍分數與計分。

## ➡️ 下一步
1. 在瀏覽器中實際測試道具生成頻率與碰撞範圍是否平衡。
2. 視需要加入音效、更多障礙類型與日夜變化。
3. 可考慮增加成就系統與難度模式。

## ⚠️ 注意事項
- 採用原生 HTML5、CSS3、JavaScript 與 Canvas API。
- PNG 素材目前以約 `82 × 82 px` 顯示；原始素材維持在 `assets/`，不要覆蓋使用者圖片。
- 未經確認不要加入大量外部依賴、後端或資料庫。

## 🕐 最後更新
- 時間：2026-09-24
- 更新者：GitHub Copilot @ Mac-mini-M2.local
- 同步方式：雲端硬碟同步（OneDrive）
